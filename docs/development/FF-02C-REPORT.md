# FF-02C Target-Machine Validation

FF-02C SOFTWARE STATUS: PASS (17/17 tests, harness runs clean, no leaks)
TARGET VALIDATION STATUS: PENDING (manual probe/overlay evidence outstanding)

> The populated per-run report is generated by `./scripts/ff02c-validate.sh`
> into the gitignored `artifacts/ff02c/FF-02C-REPORT.md`. Evidence below is a
> committed summary only. Renderer decision stays PENDING.

## Environment

status: UNKNOWN/PENDING — collected per run by the harness; visual behavior
always requires the target Wayland/Hyprland session.

## Asset inventory

status: PASS — 2026-09-25 run inventoried 5 files under `characters/local/`
(hashed in place, never uploaded):

| File | Bytes | SHA-256 |
|---|---|---|
| `Furina/source/FurinaV2.fbx` | 10,273,996 | `e1349749e0c234d777404500ab17287f33a2726a05cd72f0f4711ed18bc879b4` |
| `Furina/textures/体.png` | 2,704,092 | `50ba830d81529b64…` |
| `Furina/textures/颜.png` | 706,073 | `cd37df605cc0891f…` |
| `Furina/textures/髮.png` | 1,689,215 | `a23ee0f5769b57ca…` |
| `Furina/textures/髮2.png` | 689,369 | `222f9e3d890851d6…` |

The FBX hash matches the FF-02 record, confirming it is the same file.
(Truncated PNG hashes; full values live in the gitignored run artifacts.)

## Three.js

status: PENDING — manual browser import required (`apps/avatar-renderer/probes/ff02/`).
Zero clips from this importer alone is UNKNOWN, never NO ANIMATION.

## Godot

status: PENDING/BLOCKED — requires `godot` executable + staged copies under
ignored `probes/godot-ff02/local/`. Never auto-install the SDK from the harness.

## Texture validation

status: FAIL — 2026-09-25 run: 3 of 3 FBX-embedded texture references
(`.png`, `2.png`, `spa_h.png`) unresolved by filename match against the 4
staged CJK-named PNGs. FAIL here means the check ran and did not meet its
condition; it is not a visual-materials verdict — verify in the importer
probes. Limitation: the harness extractor reads ASCII-only strings, so
non-ASCII embedded references could be under-reported; probe evidence governs.

## Animation validation

status: UNKNOWN until both importers report. Record disagreements; pick no winner.

## Character structure

status: UNKNOWN — skeleton/bones/morphs from importer observations only.

## Performance

status: UNKNOWN — sanity snapshot only; FPS/VRAM/CPU/GPU need manual measurement.

## FF-01 overlay

status: PENDING — run `./scripts/ff01-overlay-test.sh`, inspect transparency,
layer, position, mouse, workspaces, multi-monitor, input capture.

## Ctrl+C

status: PENDING — terminal SIGINT test. `Super+C` is NOT equivalent.

## Automated checks

- environment collection, asset hashing, probe presence/dependency checks,
  Godot CLI on staged copies, texture filename-match, report scaffolding.

## Manual checks

- Three.js visual import, Godot editor inspection, overlay visuals, Ctrl+C,
  NVIDIA/Hyprland performance.

## Known limitations

- Texture report is filename matching, not a render check.
- One importer's zero clips never prove absence.
- Performance section is not a benchmark.
- Visual overlay PASS needs human observation; never faked.

## Unresolved issues

- Columbina texture state needs deterministic re-evaluation on current assets.
- Both FBXs showed 0 clips in Three.js (software sandbox); Godot side untested.
- Goth Mommy asset absent.

Renderer decision:
PENDING
