# Computer Control Security

LLM/gesture/voice → structured intent → allowlist + argument validation → OS adapter → result.

Never pass arbitrary model output to a shell. High-impact actions require confirmation. Keep an audit log and provide a global kill switch.
