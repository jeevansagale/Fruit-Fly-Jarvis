# Arch + Hyprland Target

Primary development target:
- Arch Linux
- Wayland
- Hyprland

The Linux implementation should prefer compositor-native IPC and Wayland layer-shell semantics over simulated keyboard shortcuts where possible.

The first spike is deliberately independent of the AI stack.
