[project]
name = "fruit-fly-brain"
version = "0.1.0"
description = "Local orchestration service for the Fruit-Fly desktop companion"
requires-python = ">=3.11"
dependencies = ["pyyaml"]

[project.optional-dependencies]
vision = ["mediapipe", "opencv-python"]
voice = ["faster-whisper"]

[tool.pytest.ini_options]
pythonpath = ["services"]
